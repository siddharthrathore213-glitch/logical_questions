#Basic If–Else Problems:

#1. Write a program to check whether a number is positive, negative, or zero.
n=int(input("enter any number"))
if n==0:
        print("number is zero")
elif n<0:
    print("number is negetive")
else:
    print("number is positive")

#2. Write a program to check whether a number is even or odd.
n=int(input("enter any number"))
if n==0:
    print("number is zero")
elif n%2==0:
    print("number is even")
else:
    print("number is odd")


# 3. Write a program to check if a given year is a leap year or not.
n=int(input("enter a year"))
if n%4==0:
 if n%100==0:
    if n%400==0:
       print(f"{n} is a leap year")
    else:
        print(f"{n} is a not leap year")
 else:
     print(f"{n} is a leap year")
else:
     print(f"{n} is a not leap year")
    
    

#4. Write a program to find the greatest of two numbers.
a, b =map(int,input("enter two number").split())
if a>b:
    print(f'{a} is grater than {b}')
elif a==b:
    print(f'{a} is equal to {b}')
else:
    print(f'{b} is grater than {a}')

#5. Write a program to check whether a person is eligible to vote (age >= 18).
age=int(input("enter your age"))
if age>= 18:
    print('eligible to vote')
else:
     print('not eligible to vote')

#6. Write a program to check whether a given character is a vowel or consonant.
char = input("enter a charachter")
li = ['a','e','i','o','u']
if char in li :
    print(f'{char} is a vowel')
else:
    print(f'{char} is a consonant')

#7. Write a program to check if a number is divisible by 5.
n=int(input("enter any number"))
if n%5==0:
     print(f'{n} is divisible by 5')
else:
     print(f'{n} is not divisible by 5')


# 8. Write a program to determine whether a given number is a single-digit, two-digit, or more than two-digit
#   number.
n=int(input("enter any number"))
if n in range (-9,10):
    print('given number is single digit')
elif n in range(-99,-9) or n in range (10,100):
    print('given number is double digit')
else:
    print('given number has more than two digit')

#9. Write a program to check whether a student has passed or failed (passing marks = 40).
mark = int(input("enter your marks"))
if mark<40:
    print('student has failed')
else:
    print('student has passed')
    

#10. Write a program to find whether the entered number is a multiple of both 3 and 7.
n=int(input("enter any number"))
if n%3==0 and n%7==0:
    print(f'{n} is a multiple of both 3 and 7')
else :
    print(f'{n} is not a multiple of both 3 and 7')

Ladder If & Nested If:

#1. Write a program to find the greatest among three numbers. 
num1 ,num2 ,num3 = map(int,input('enter three numbers').split())
if num1>num2 and num1>num3:
    print(f'{num1} is greatest amoung given numbers.')
elif   num2>num1 and num2>num3: 
     print(f'{num2} is greatest amoung given numbers.')
elif   num3>num1 and num3>num2:
    print(f'{num3} is greatest amoung given numbers.')
else :
    print('all numbers are equal')

#2. Write a program to classify a person based on age:
#   Child (<13), Teenager (13-19), Adult (20-59), Senior (60+). 
age = int(input('enter your age'))
if age < 13:
    print("you are classified as a child")
elif age in range (13,20)  :
    print("you are classified as a teenager")
elif age in range (20,60):
    print("you are classified as a Adult")
else :
    print("you are classified as a senior")

#3. Write a program to assign grades based on marks: 
#90-100: A,  
#75-89: B,  
#50-74: C,  35-49: D,  <35: Fail. 
marks = int(input('enter marks'))
if marks in range (90,101):
    print('Grades = A')
elif marks in range (75,90):
    print('Grades = B')
elif marks in range (50,75):
    print('Grades = C')
elif marks in range (35,50):    
    print('Grades = D')
elif marks<35:
    print('Failed9876')
else :
    print('enter correct marks between 0 to 100')

#4. Write a program to check the type of triangle (equilateral, isosceles, or scalene) based on sides.
sid1,sid2,sid3 =  map(int,input('enter length of each side of triangles').split())
if sid1==sid2 and sid1==sid3:
    print('Triangle is equilateral')
elif sid1==sid2 or sid1==sid3 or sid2==sid3 :
    print('Triangle is Isosceles')
else    :
    print('Triangle is Scalene')

#5. Write a program to check if a character is uppercase, lowercase, digit, or special symbol.
char = input('enter a single character')
if len(char)<=1:
  if char.isdigit() :
    print('given character is a digit')
  elif char.islower():
    print('given character is a Lowercase')
  elif char.isupper():
    print('given character is a Uppercase')
  else:
    print('given character is a special symbol')  
else:
    print('enter a valid single character')
    
 

#6. Write a program to calculate electricity bill based on units: 
#Up to 100 units: ₹5 per unit, 
#101–200 units: ₹7 per unit, 
#Above 200 units: ₹10 per unit. 
unit= int(input('enter units used'))
amt = 0
if unit in range(0,101):
    amt = unit*5 
    print(f'bill amount is {amt}')
elif unit in range(101,201):
        amt = (unit-100)*7+500
        print(f'bill amount is {amt}')  
elif unit>200:
            amt= (unit-200)*10+1200
            print(f'bill amount is {amt}')      
else:
    print('enter a valid number of units')
    

#7. Write a program to determine the largest of four numbers using nested if.
num1,num2,num3,num4 = map(int,input('enter four numbers').split())
if num1>num2:
    if num1>num3:
        if num1>num4:
            print(f'{num1} is gratest')
        else:    
            print(f'{num4} is gratest')
    elif num3>num4:
        print(f'{num3} is gratest')
    else:
        print(f'{num4} is gratest')        
elif num2>num3:
    if num2>num4:
        print(f'{num2} is gratest')
    else:
        print(f'{num4} is gratest')
elif num3>num4:
    print(f'{num3} is gratest')
else:
    print(f'{num4} is gratest')

#8. Write a program to check if a given year is a century year and also a leap year.
n=int(input("enter a year"))
if n%4==0:
 if n%100==0:
    if n%400==0:
       print(f"{n} is a leap year as well as a century year")
    else:
        print(f"{n} is a century year but not a leap year")
 else:
     print(f"{n} is a leap year but not a century year ")
else:
      print(f"{n} is a not a leap year nither a century year")
    
#9. Write a program to classify BMI value: Underweight (<18.5), Normal (18.5-24.9)
#  , Overweight (25-29.9),  Obese (30+). 
import numpy as np
bmi= float (input('enter a BMI value'))
if bmi<18.5:
    print('Classified as underweight')
elif 18.5<=bmi<25.0 :
    print('Classified as Normal')
elif 25<=bmi<30.0 :
    print('Classified as overweight')
else:
    print('Classified as Obese')

#10. Write a program to display the smallest number among three using nested if. 
num1,num2,num3,num4 = map(int,input('enter four numbers').split())
if num1<num2:
    if num1<num3:
        if num1<num4:
            print(f'{num1} is smallest')
        else:    
            print(f'{num4} is smallest')
    elif num3<num4:
        print(f'{num3} is smallest')
    else:
        print(f'{num4} is smallest')        
elif num2<num3:
    if num2<num4:
        print(f'{num2} is smallest')
    else:
        print(f'{num4} is smallest')
elif num3<num4:
    print(f'{num3} is smallest')
else:
    print(f'{num4} is smallest')
For Loop Problems:
#1. Write a program using a for loop to print all Armstrong numbers between 100 and 999.
#  (Armstrong number:  sum of cubes of digits equals the number itself. Example: 153 => 1³+5³+3³ = 153).
print ('following are armstrong numbers between 100 and 999')
for digits in range(100, 1000):
  a=0
  for i in str(digits):
    a += int(i)**3
  if  a==digits:
   print(digits)

#2. Write a program to generate and display the first n prime numbers using a for loop. 
n=int(input('enter a end range for prime number generation'))
print(f'first {n} primes are')
i=2
k=0
while k<n:
        a=0
        for j in range(1,i+1):
            if i%j == 0:
                a+=1
        if a == 2 :
             print(i)
             k+=1
        i+=1

#3. Write a program to display all numbers from 1 to 500 that are divisible by 3,
#   but the sum of their digits  should not exceed 10.
for i in range(1,501):
    if i%3==0:
        a=0
        for k in str(i):
            a+=int(k)
        if a<=10:
            print(f'{i}')
    


#4. Write a program using a for loop to print a pyramid of stars (*) of height n. Example for n=4:  
                          
#   * 
#  *** 
# ***** 
#******* 
n= int(input('enter height of pyramid')) 
for j in range (1,n+1):
        print(" "*(n-j)+'*'*(2*j-1))
       

#5. Write a program to accept a string and check whether it is a pangram (contains all 26 alphabets at least once)  using a for loop. 
ch= input('enter a sentence')
li=[]
a=0
for i in ch.lower():
   if i not in li:
       li.append(i)
       
   else :
    continue
for i in li:
    if not i.isalpha():
        li.remove(i)
if len(li)==26:
    print('it is a pangram')
else:
    print('it is not a pangram')
    

#6. Write a program using a for loop to print all twin primes between 1 and 100.
#   (Twin primes: pairs of prime  numbers with a difference of 2, e.g., (3,5), (11,13)). 
i=2
k=0
li=[]
for k in range(1,101):
        a=0
        for j in range(1,i+1):
            if i%j == 0:
                a+=1
        if a == 2 :
             li.append(i)
             k+=1
        i+=1
for j in li:
  if li.index(j)<len(li)-1:
     k= li.index(j)+1
     if li[k]-j==2:
        print(f"{j} , {li[k]} ")
    
    


#7. Write a program that accepts a number from the user and prints whether it is a Harshad number (number  divisible by the sum of its digits) using a for loop. 
num = input('enter a number')
a=0
for i in num:
    a += int(i)
if  int(num)%a==0:
   print(f'{num} is a harshad number')
else :
    print(f'{num} is not a harshad number')

#8. Write a program to generate Pascal’s Triangle up to n rows using a for loop. 
n= int(input('enter number of rows')) 
for i in range (0,n+1):
    for j in range (0,n-i):
         print(' ',end='')
    C = 1
    for j in range(1, i+1):
         print(C, ' ', sep='', end='')
         C = C * (i - j) // j
    print()

#9. Write a program using a for loop to display the sum of the series: 
#   1² + 2² + 3² + ... + n² 
n = int (input('enter nth term of the series'))
k=0
for i in range(1,n+1):
    k+=i**2
print(f'sum of series is {k}')    

#10. Write a program that accepts a number from the user and prints whether it is a Strong number 
#   (sum of  factorials of digits = number itself) using a for loop. Example: 145 => 1! + 4! + 5! = 145.
num = int(input('enter a number'))
sum1=0
for i in str(num):
      a=1    
      for k in range (1,int(i)+1):
          a*= k
      sum1+=a 
if sum1==num:
    print('it is a strong number')

else:
    print('it is not a strong number')
#11. Write a program using a while loop to find the reverse of a number and check if the reversed number is  prime. Example: Input = 73 → Reverse = 37 → Prime.
num= int(input('enter a number'))
num2=0
while num >0:
    last= num%10
    num2 = num2*10+last
    num//=10
print (f'reverse : {num2}')   
n=1
a=0
while n <= num2:
    if num2%n==0:
        a+=1
    n+=1
if a==2:
    print('it is a prime number' )
else:
      print('it is not a prime number' )

#12. Write a program that continues to accept numbers from the user until the sum of digits of all numbers  entered becomes greater than 100. 
n=0
while n<=100:
    a = int(input('enter a number'))
    di= sum(int(i) for i in str(a))
    n+=di
print(f'sum of entered number is {n} , it exceeds 100')    

#13. Write a program using a while loop to check whether a number is a Duck number (a number containing zero  but not starting with zero, e.g., 202, 1203). 
num= input('enter a number')
n=1
zero = False
while n< len(num):
    if num[n]=='0':
        zero = True
    n+=1
if zero == True :
    print('Duck number')
else :
    print('Not a Duck number')
    
#14. Write a program using a while loop to accept a number and check if it is a Happy number.
#(A number is  happy if repeatedly replacing it with the sum of squares of its digits eventually reaches 1). Example: 19 is a  happy number. 
num = int(input("Enter a number: "))
li = []
while num!=1 and num not in li:
    li.append(num)
    sum1=0
    for i in str(num):
        sum1+=int(i)**2
    num =sum1
if num == 1:
    print('Happy number')
else:
    print('Not a Happy number')
    
#15. Write a program using a while loop to find the largest prime factor of a given number. 
num = int(input("Enter a number: "))
i = 2
largest = 0

while i * i <= num:
    while num % i == 0:
        largest = i
        num //= i
    i += 1
if num > 1:
    largest = num

print("largest Prime factors:", largest)

#16. Write a program to repeatedly accept a string from the user until the string entered is a palindrome.
n=True
while n ==True:
     s = input('enter a string')
     s1= s[::-1]
     if s==s1:
         n=False
print('palindrom entered')         

#17. Write a program using a while loop to compute the sum of digits of a number until the result becomes a  single-digit number (Digital root). 
#   Example: 9875 => 9+8+7+5=29 => 2+9=11 => 1+1=2. 
num = input('enter a number')
while len(num)>1:
    sum1 = 0
    for i in num:
        sum1+=int(i)
    print (f'sum of digits of {num} is',sum1)       
    num = str (sum1)    
    
#18. Write a program using a while loop to generate the Collatz sequence for a given number. 
#    (Rule: If n is even  => n/2, if odd => 3n+1. Continue until n=1). 
num = int(input('enter a number'))
print('collats sequence for',num)
while num!=1:
    if num%2==0:
        print(num//2, sep=' ',end=' ')
        num //=2
    else:
        print(3*num+1, sep=' ',end=' ')
        num=num*3+1

#19. Write a program using a while loop to accept a number and check whether it is a Kaprekar number.
#   (Kaprekar number: if square of the number can be split into two parts whose sum equals the number.  Example: 45²=2025 => 20+25=45)
num = int(input('enter a number'))
num2=str(num**2)
n=1
while n<len(num2):
         str(num2)
         a = num2[0:n] 
         b = num2[n:] 
         if int(a)+int(b) == num:
                    print(f"It is a Kaprekar number: {num}² = {num2} → {a} + {b} = {num}")
                    break
         n+=1    
   
if int(a)+int(b) != num:
    print('not a karprekar number')

#20. Write a program to simulate an ATM machine using a while loop where a user can: 
#• Check balance • Deposit money 
#• Withdraw money (only if balance is sufficient) • Exit 
#Continue until the user chooses to exit. 
balance =0
while True:
    print("1. Check Balance")
    print("2. Deposit Money")
    print("3. Withdraw Money")
    print("4. Exit")
    opp = input('choose a opption (1-4)')
    if opp == '1':
        print(f"Your current balance is ₹{balance}")
    elif opp == '2':
        amt = int(input('enter deposit amount: '))
        if amt>0:
            balance+=int(amt)
            print(f"₹{amt} deposited. New balance: ₹{balance}")
        else :
            print('invalid deposit')
    elif opp=='3':
        wdr=int(input('enter withdrawl amount '))
        if wdr>balance:
             print("Insufficient balance.")
        else:
            balance -= wdr
            print(f"₹{wdr} withdrawn. Remaining balance: ₹{balance}")
    elif opp=='4':
        print('thank you')
        break
    else:
        print('enter valid opption')
